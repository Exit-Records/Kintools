[build]
  base      = "artifacts/kin-qr-builder"
  command   = "pnpm install --frozen-lockfile && pnpm run build"
  publish   = "dist/public"

[build.environment]
  BASE_PATH = "/"
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
